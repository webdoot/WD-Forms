/**
 * Admission form ajax
 */

jQuery(document).ready(function($){

  $('button.wdfms-form-tmpl').click(function(){ 
        var postid = $(this).attr("data-id");

        console.log(postid);

  });

  
});
